# Docker Compose Environment Variables
# Copy to .env and configure before running

# ============================================
# Google OAuth (required for production auth)
# ============================================
# Get credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_ALLOWED_DOMAIN=yourdomain.com

# OAuth callback URL (update for production)
AUTH_CALLBACK_URL=http://localhost:3001/api/auth/google/callback

# ============================================
# Session Security
# ============================================
# Generate with: openssl rand -hex 32
SESSION_SECRET=change-me-in-production-use-random-string

# ============================================
# Home Assistant Integration
# ============================================
# Webhook token for HA automation validation
HA_WEBHOOK_TOKEN=your-webhook-secret

# ============================================
# Kiosk Configuration
# ============================================
# QR token expiry in seconds (default: 60)
TOKEN_EXPIRY_SECONDS=60

# ============================================
# External APIs
# ============================================
# Get free key at: https://pixabay.com/api/docs/
PIXABAY_API_KEY=your-pixabay-key

# ============================================
# Cron Jobs
# ============================================
# Secret token for authenticating cron job API calls
# Generate with: openssl rand -hex 32
CRON_SECRET=your-cron-secret-here

# ============================================
# URLs (update for production)
# ============================================
# PWA URL for OAuth redirects
PWA_URL=http://localhost:5173
